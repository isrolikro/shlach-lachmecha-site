import { Routes } from '@angular/router';
import { ArchiveBrowserComponent } from './features/archive-browser/archive-browser.component';

export const routes: Routes = [
    { path: '', component: ArchiveBrowserComponent },
];
